# Raytracing in a Weekend

This is my implementation of [Raytracing in a Weekend](https://raytracing.github.io/books/RayTracingInOneWeekend.html). It uses `js_of_ocaml` to generate a static website running all the code. 

## Dependencies
This project relies on the unreleased library htmlact. `opam pin add` htmlact, htmlit and webs by Bünzli.
